# 第一关插排与计时修复

2026-09-23，Unity 6000.6.0f1。

## 原因与修复

- 主菜单加载 `HMP_game/Assets/Levels/level_1_initial_fire/Scenes/level_1_initial_fire.unity`。`Assets/Scenes/办公室场景-实体试点.unity` 是独立保留场景，对它的编辑不会自动进入正式第一关。本次仅同步其中 `SM_Hazard_OverloadSocket_01_Aged` 的位置、旋转和缩放，保留试点场景的其他编辑。
- 原来的答题对象在另一只 `SM_Choice_PowerStrip` 下，和起火插排不是同一物体。本次隐藏答题替身，将原交互实体保留并显式关联可见插排；交互范围随可见插排更新，提示锚点、导航接近点跟随同一插排。实体 ID 和题目目标 ID 不变。
- 第三题 `freezeFire=true` 原本同时申请了整个 session 的 Simulation 冻结，导致题目倒计时不动。现在火势冻结只暂停火焰和阶段计时；题目倒计时继续。用户暂停仍冻结整个 session。

## 后续编辑

优先在正式第一关场景调整插排根物体并保存，再从初始界面启动验证。不要只移动插排内部的插头子网格。

如果仍需把试点场景里保存的插排姿态同步到正式关卡，退出 Play、保存打开的场景后运行 `Tools → Levels → Sync Power Strip From Entity Pilot`。此工具明确执行一次同步，不在运行时自动覆盖位置。

## 验证

在完整隔离工程副本中编译并从真实主菜单选关，使用真实选择按钮、物理射线及键盘 E 输入，未直接调用答案完成函数。

- [修复前报告](验证记录/第一关插排与计时修复/before.txt)：前两题正常，第三题等待 0.6 秒仍为 45 → 45，复现计时冻结。
- [修复后报告](验证记录/第一关插排与计时修复/after.txt)：全部通过。第三题俯视出题及第一人称操作期间均正常倒计时；实际暂停可停止时钟；瞄准可见插排的中心按 E 完成答题；11 阶段、3/3 成功结算、重试、错误操作、超时去重、第三题再次交互及返回主菜单通过。

交互回归会将玩家定位到导航接近点，再通过碰撞移动进入范围；不等同于完整人工步行和渲染观感验收。本次为 headless Editor 运行，未构建独立 Player。

运行回归：`Tools → Levels → Check Initial Fire Lua Migration`。新增 `OfficePowerStripRepair` 负责显式场景迁移；扩展 `OfficeLuaMigrationChecks` 覆盖上述计时及可见物体交互。
